{# if form #}
    {{ form_start(form) }}
    <div>
            <div class="flex justify-center">
                {{ form_widget(form.q) }}
            </div>
    </div>
    {{ form_end(form) }}
{# endif #}

