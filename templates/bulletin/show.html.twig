{% extends 'base.html.twig' %}

{% block title %} Détail du {{bulletin.eleve}} {% endblock %}

{% block body %}

    <section class="container">
            <div>
                {{bulletin}}
            </div>
            {# je recupere tous les bulletinGroupeCompetences (objet) dans le bulletin #}
            {% for bgc in bulletin.bulletinGroupeCompetences  %}
                {{dump(bgc)}}
                {# Dans ce groupe, j'affiche chaque bulletinGroupeCompetences (objet) #}
                {{ bgc.groupeCompetences }}
                    test
                    {# groupe de competence : {{ gc.titre }} #}
            {% endfor %}

    </section>

{% endblock %}