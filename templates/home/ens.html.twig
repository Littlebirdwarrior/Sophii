{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}


{% block body %}

    <main>
        <section class="container">
            <p>Mon profil {{ app.user.stringRoles }} - Année {{ "now"|date_modify("-1 year")|date("Y") }}
                /{{ "now" | date("Y") }}</p>
            <h1>Bienvenue <span class="text-primary"></span>{{ app.user.prenom }}</h1>
            <article class="homebox">

                <a href="{{ path('app_classe') }}" class="homecard bg-1">
                    <p> Ma classe</p>
                </a>

                <a href="{{ path('app_activite') }}" class="homecard bg-2">
                    <p> Mes activités</p>
                </a>

                <a href="{{ path('app_competence') }}" class="homecard bg-3">
                    <p> Mes compétences</p>
                </a>

                <a href="{{ path('app_feuille_route') }}" class="homecard bg-4">
                    <p> Mes feuilles de route</p>
                </a>

                <a href="{{ path('app_parents') }}" class="homecard bg-5">
                    <p> Ma liste parents</p>
                </a>

                <a href="{{ path('app_competence') }}" class="homecard bg-1">
                    <p> Mon profil</p>
                </a>

            </article>
            {% include 'home/_contact.html.twig' %}
        </section>
    </main>
{% endblock %}
