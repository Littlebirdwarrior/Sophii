{% block header %}
    <nav class="navpanel">

        <ul>
            <li>
                <a href="{{ path('app_famille') }}">Home</a>
            </li>
            <li>
                <a href="{{ path('app_famille') }}">Ma famille</a>
            </li>
            <li>
                <a href="{{ path('app_activite') }}">Les activités</a>
            </li>
            <li>
                <a href="{{ path('app_user') }}">Fiche info enseignant</a>
            </li>
            <li>
                <a href="{{ path('app_feuille_route') }}">Feuille de route</a>
            </li>
        </ul>
        <ul class="connexion">
            {% if app.user %}
                <li>
                    <a href="#">Profil</a>
                </li>
                <li>
                    <a href="{{ path('app_logout') }}">Déconnexion</a>
                </li>
            {%endif %}
            {% if not app.user %}
                <li>
                    <a href="{{ path('app_login') }}">Se connecter</a>
                </li>
                <li>
                    <a href="{{ path('app_register') }}">S'inscrire</a>
                </li>
            {% endif%}
        </ul>
    </nav>
{% endblock %}
