{% extends "base.html.twig" %}

{% block body %}
<section class="container form-page">
    <h1>Ajouter une classe</h1>
    {{form_start(formAddClasse)}}
        {{form_row(formAddClasse.niveau)}}
        {{form_row(formAddClasse.libelle)}}
    {{form_end(formAddClasse)}}
</section>

{% endblock %}